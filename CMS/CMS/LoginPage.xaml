<Page
    x:Class="CMS.LoginPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CMS"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

  <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <StackPanel x:Name="LoginStackPanel" HorizontalAlignment="Center" VerticalAlignment="Center" Width="250">
      <Button Click="Logo_Clicked" HorizontalAlignment="Center">
        <Button.Template>
          <ControlTemplate TargetType="Button">
            <ContentPresenter/>
          </ControlTemplate>
        </Button.Template>
        <Image Source="https://i.vgy.me/jvvcFQ.png" Width="160" Height="100" Margin="0,10"/>
      </Button>
      <TextBlock Text="Login" VerticalAlignment="Center" FontSize="20" TextAlignment="Center" Margin="0,10" />
      <TextBlock Text="Email" Margin="0,10,0,0"/>
      <TextBox Name="EmailField" />
      <TextBlock Text="Password" Margin="0,10,0,0"/>
      <PasswordBox Name="PasswordField" />
      <Button Content="Login" Margin="0,20,0,5" Click="Login_Clicked" HorizontalAlignment="Center"/>
      <TextBlock HorizontalAlignment="Center">
        <Run Text="Don't Have an Account?" />
        <Hyperlink Click="Signup_Clicked">Sign Up</Hyperlink>
      </TextBlock>
    </StackPanel>
  </Grid>
</Page>
